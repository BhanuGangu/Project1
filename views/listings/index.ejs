<% layout("/layouts/boilertemplate") %>

<div id="fliters" class="mt-3">
    <div class="fliter">

    </div>
</div>

    <body>
        <div class="mt-12">

        </div>
        <!-- <h3>All Listings</h3> -->
        <!-- <form method="GET" action="/listings/new">
        <button>Create New Listing</button>
    </form> -->
        <ul>
            
            <div class="row row-cols-1 row-cols-md-3 g-4" >
                <% for(let l of all_listings){ %>
                <a href="/listings/<%= l._id %>" class="listing-link">
                    <div class="card listing-card">
                        <img src="<%= l.image.url %>" class="card-img-top" alt="listing_img" style="height: 20rem;">
                        <div class="card-img-overlay"></div>
                        <div class="card-body">
                           <p class="card-text"><%= l.title %><br> &#8377; <%= l.price ? l.price.toLocaleString("en-IN") : "Price not avaliable" %> / night </p>
                        </div>
        
                    </div>
                </a>    
                    <% } %>
            </div>
            

            <!-- <% for(let l of all_listings){ %>
                <li><a href="/listings/<%=l._id %>">
                        <%= l.title %>
                    </a></li>
                <% } %> -->
        </ul>

    </body>